

@model ArtistManagement.Models.Role
<div class="row mb-2">
    <div class="col-10 mt-2">
        @Html.DropDownListFor(x => x.RoleId, ViewBag.roles as SelectList, "----Select----", htmlAttributes: new { @class = "form-select form-select-sm" })
    </div>
    <div class="col-2">
        <a href="#" id="btnDelete" class="btn btn-danger btn-sm mt-2"><i class="fa fa-trash"></i>Delete</a>
    </div>
</div>
